HOW TO:
SPLIT:
	Drag a single file onto the executable and the program will treat it as an argument. After that just follow the prompts.
JOIN:
	Drag multiple files onto the executable and the program will treat them all as arguments, joining them.